import * as React from "react";

const Header = () => {
  return (
    <p id="header" className="jumbotron text-center headTitle">
      CLICK A COLOR
    </p>
  );
};

export default Header;
